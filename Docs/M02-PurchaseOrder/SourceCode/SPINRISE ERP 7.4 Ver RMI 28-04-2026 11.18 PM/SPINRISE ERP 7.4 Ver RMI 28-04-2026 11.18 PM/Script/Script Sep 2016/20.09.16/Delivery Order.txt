CREATE TABLE RM_DOHD (DIVCODE VARCHAR(2),DOCNO VARCHAR(10),
					  DOCDATE DATETIME,
					  SLCODE VARCHAR(7),
					  TRAUCKNO VARCHAR(15),
					  CARRIERCODE VARCHAR(10),
					  CREATEDBY VARCHAR(50),
					  MODIFYBY VARCHAR(50),
					  CREATED_DATE DATETIME,
					  MODIFY_DATE DATETIME)
					  
CREATE TABLE RM_DODT(DIVCODE VARCHAR(2),DOCNO VARCHAR(10),
					  DOCDATE DATETIME,
					  CONTNO VARCHAR(10),
					  CONTDT DATETIME,
					  PRNO VARCHAR(20),
					  QUANTITY NUMERIC(10,0))
