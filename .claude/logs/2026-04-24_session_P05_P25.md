# Session Log — P05–P25 Batch Execution
**Date**: 2026-04-24  
**Branch**: master  
**Session type**: CEO UI Review + FSD task batch

---

## Tasks Completed

### P05 — Auto-calculate approxCost from rate × qty
- `PRLineItemsTable.tsx`: Both `handleAddOrUpdate` branches set `approxCost` to `rate * qtyRequired` (2 dp) when both fields are present, else null.

### P06 — ViewModal: Tooltip truncation + DrawNo column
- `PRViewModal.tsx`: Wrapped Description and Remarks columns in `Tooltip`. Added `Draw No` column (width 90) between Item Name and Cat No.

### P07 — ViewModal: Approx Cost column with fallback
- `PRViewModal.tsx`: Added `Approx. Cost` column rendering `approxCost` if > 0, else `lastPoRate * qtyRequired` as fallback.

### P08 — Approval status display in header
- `PRHeaderCards.tsx`: Added 9 new props (budgetValidationEnabled, budgetBalance, approvalVisible, level1/2/finalApproverName, level1/2/finalApprovedAt). Renders L1/L2/Final approval tags gated by `approvalVisible && any approver present`.

### P09 — PENDING status in Create page stepper
- `PurchaseRequisitionV2Page.tsx`: Added `PENDING: 1` to `statusToStep` map.

### P10 — Edit page migrated to v2 components
- `PurchaseRequisitionEditPage.tsx`: Replaced `PRHeaderV2` + `PRItemFormV2` + `PRItemTableV2` with `PRHeaderCards` + `PRLineItemsTable`. Added `warnings` state + warning Alert display. Added `drawNo`/`catNo` to `buildPayload` (were previously missing). Added all budget + approval props.

### P11 — SubCostCode in Advanced Drawer
- `PRLineItemsTable.tsx`: Added `subCostCode` InputNumber Form.Item in Advanced Drawer (before categoryCode).

### P12 — Budget balance display in header
- `PRHeaderCards.tsx`: Budget balance `Tag` (green/red) showing ₹ formatted amount, gated by `budgetValidationEnabled`.

### P13 — Per-line deletion in table
- `PRLineItemsTable.tsx`: Added `savedPrNo?: number` and `deleteReasons` props. 3 state vars for line delete modal. Action column conditionally shows delete-reason modal (when `savedPrNo` present) vs simple Popconfirm. Per-line delete calls `purchaseRequisitionApi.deleteLine(prNo, prSNo, reason)`.
- `types.ts`: Added `prSNo?: number` to `PRLineFormItem`.
- `PurchaseRequisitionV2Page.tsx` + `PurchaseRequisitionEditPage.tsx`: Pass `savedPrNo` and `deleteReasons` props.

### P14 — GIN No / Requisition Type
- Assessment: Requires new DB columns in `po_prh`. Deferred — no partial implementation.

### P15 — Item Group filter
- `types.ts`: Added `itemGroup?: string` to `ItemLookup` and `PRLineFormItem`.
- UI filtering deferred (needs `itemGroupFilterEnabled` flag + `searchItems` API param change).

### P16 — Manual PR number
- Assessment: Requires backend skip of `GetNextPrNumberAsync` + uniqueness check. Deferred.

### P17 — SubCostCode backend validation
- `PurchaseRequisitionService.cs`: Added `SubCostNotFound` message. Added SubCostCode validation in both `ValidateCreateLinesAsync` and `ValidateUpdateLinesAsync` (V26).
- `IPurchaseRequisitionRepository.cs`: Added `SubCostExistsAsync(divCode, subCostCode)`.
- `PurchaseRequisitionRepository.cs`: Implemented `SubCostExistsAsync` via `ksp_PR_SubCostExists` SP.
- `Constants.cs`: Added `SubCostExists = "ksp_PR_SubCostExists"`.
- `ksp_PR_ExistsChecks.sql`: Added `ksp_PR_SubCostExists` querying `in_scc WHERE DIVCODE+SCCCODE+active='Y'`.
- `merged.sql`: Updated with new SP.
- Build: CLEAN (no CS errors).

### P18/P19 — Rate History Modal + SP
- Already fully implemented. `PRRateHistoryModal.tsx` is self-contained; `ksp_PR_GetItemHistory.sql` is complete. No changes needed.

### P20 — Item search shows pending qty warnings
- `PRLineItemsTable.tsx`: Item search option labels append `[PR:x]` and `[PO:x]` when pending quantities > 0.

### P21 — Header status derivation
- Already correct. SP `ksp_PR_GetById` derives 'RECEIVED' via CASE WHEN on line-level `prstatus='C'` field. No changes needed.

### P22 — FY calc + post-save reload
- Already correct. FY calc is `month >= 3 ? year : year - 1` (April–March). Post-save reload already in `doSave`. No changes needed.

### P23 — Category code storage
- Already implemented. `ksp_PR_InsertLine.sql` inserts to `po_prl.CATCODE`. `PRMappings` already maps `CategoryCode`. No changes needed.

### P24 — Scope Code field in header
- `PRHeaderCards.tsx`: Added `scopeCode` Input (maxLength=10) to Row 3 alongside Section and Order Type.

### P25 — DTO field length corrections
- `CreatePRHeaderDto.cs`: `IType` `[StringLength(2)]` → `[StringLength(1)]`.
- `CreatePRLineDto.cs`: `Remarks` `[StringLength(50)]` → `[StringLength(100)]`.

---

## Files Modified

### Frontend
- `src/features/purchase-requisition/components/v2/PRLineItemsTable.tsx`
- `src/features/purchase-requisition/components/v2/PRHeaderCards.tsx`
- `src/features/purchase-requisition/components/v2/PRRateHistoryModal.tsx` (P01 currency)
- `src/features/purchase-requisition/components/pr-list/PRViewModal.tsx`
- `src/features/purchase-requisition/pages/PurchaseRequisitionV2Page.tsx`
- `src/features/purchase-requisition/pages/PurchaseRequisitionEditPage.tsx`
- `src/features/purchase-requisition/types.ts`

### Backend
- `Spinrise.Application/Areas/Purchase/PurchaseRequisition/Services/PurchaseRequisitionService.cs`
- `Spinrise.Application/Areas/Purchase/PurchaseRequisition/Interfaces/IPurchaseRequisitionRepository.cs`
- `Spinrise.Application/Areas/Purchase/PurchaseRequisition/DTOs/CreatePRHeaderDto.cs`
- `Spinrise.Application/Areas/Purchase/PurchaseRequisition/DTOs/CreatePRLineDto.cs`
- `Spinrise.Infrastructure/Areas/Purchase/PurchaseRequisition/PurchaseRequisitionRepository.cs`
- `Spinrise.Shared/Constants.cs`
- `Spinrise.DBScripts/Scripts/02 Stored Procedures/ksp_PR_ExistsChecks.sql`
- `Spinrise.DBScripts/merged.sql`

---

## Deferred (out of scope / needs DB schema changes)
- **P14**: GIN No + Requisition Type — requires new `po_prh` columns
- **P15 UI**: Item Group filter UI — type fields added, filter UI not implemented
- **P16**: Manual PR number — requires backend uniqueness check logic
